# Dev / test dependencies. Not installed in the production image.
#   pip install -r requirements-dev.txt
-r requirements.txt
pytest>=8.0
httpx>=0.27          # backend for starlette.testclient (TestClient)
