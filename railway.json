{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "python scripts/sync.py --init-db && python scripts/sync.py --domain all --mode incremental --email",
    "cronSchedule": "0 8 * * *",
    "restartPolicyType": "NEVER",
    "numReplicas": 1
  }
}
